# ── 캐시 / 로그 ──
__pycache__/
*.pyc
*.log

# ── 민감: SGIS 로그인 쿠키 캡처 (절대 커밋 금지) ──
*.har
sgis_capture.har

# ── 대용량 원시 SGIS 데이터 (SGIS 자료제공에서 받아 ①에 넣는 것 — 리포엔 안 올림) ──
260412_SGIS_베이스자료/
260413_SGIS_베이스자료/
0706테스트용/

# ── 검증용 골든 데이터 (내부자료 유림_17시) — 필요 시 수동 복사 ──
data/

# ── 생성 산출물 ──
검증결과_전주.xlsx
out/
*.bak

# SGIS 쿠키(민감) 절대 커밋 금지
cookie.txt
sgis_probe_region.py
sgis_probe_download.py

# ── 검토용 샘플/작업 엑셀 · 엑셀 잠금 임시파일 (커밋 금지) ──
샘플_*.xlsx
~$*.xlsx
*statistics_code.xlsx

# 정본 양식(데이터 포함)·대용량 산출물 — 공개리포 제외
복합쇠퇴진단.xlsx
쇠퇴진단_전국출력*/
*.pkl
